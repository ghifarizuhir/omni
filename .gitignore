node_modules/
build/
dist/
coverage/
.DS_Store
*.log
nohup.out
.env*
!.env.example

# Local SQLite dev DB (regenerated via `npm run db:reset`)
prisma/*.db
prisma/*.db-journal

# Claude Code local config / worktrees
.claude/

# Local docker compose override (secrets, dev tweaks). See
# compose.override.yml.example for the starter template.
compose.override.yml
