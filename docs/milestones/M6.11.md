# M6.11 — Write-endpoint backlog remediation

**Parent:** [M6](./M6.md) · **Audit:** [`docs/audits/mutation-audit.md`](../audits/mutation-audit.md)

## Goal

Close the gap the M6.4 audit revealed: 127 mutation handlers across the UI touch local React state only. Wire every one to a server endpoint that validates, persists, audits, and surfaces side-effects (timeline events, status transitions, notifications) — using the pattern locked in by the incident-resolve demo slice.

## Pattern (locked)

Each write endpoint follows the same six-step pattern, established in the incident-resolve slice (M6.11 demo) and re-applied in changes/requests:

1. **Shared Zod schema** — `src/shared/schemas/<domain>.ts` (or extend existing). Both server route and client service import from there.
2. **Repo function** — single transaction: validate → snapshot → update + side-effect rows → return `{ before, after, internalId }`.
3. **Route** — `requirePermission('<key>.write')` (or `.resolve`/`.publish` for verbs already in catalog) → Zod parse → repo call → audit log → respond with updated snapshot.
4. **Service method** — typed against shared schema.
5. **UI rewire** — capture `refresh` from `useResource`; replace `setState`-only handler with optimistic local update → `await service.fn(...)` → `refresh()` (revert-on-failure for snappy modals).
6. **Vitest suite** — covers auth (401), validation (400), not-found (404), happy path (200), GET round-trip, side-effects (timeline / audit log), and 403 for caller missing the permission.

## Execution strategy

The backlog is ~40 endpoints across 7 domains. They're mostly independent — same pattern, different files. Subagent-driven (one implementer subagent per task + spec reviewer + code quality reviewer per task) is the right fit. Tasks below are batched so each is right-sized for a single implementer pass (one domain, one file family).

### Batch 1 (this milestone)

The audit's "Top 5 critical" closes once **B1.1** lands; **B1.2** clears the last 🟠 (unguarded) handlers from the audit; **B1.3** is the only domain still at 0%; **B1.4** rolls up the remaining IncidentDetail handlers; **B1.5** unblocks KB.

| # | Subject | Endpoints | UI | Permission key(s) | Audit reference |
|---|---------|-----------|----|-------------------|------------------|
| B1.1 | AlertRouting writes | `POST /monitoring/alert-routes`, `PATCH /monitoring/alert-routes/:publicId`, `DELETE /monitoring/alert-routes/:publicId` | `AlertRouting.tsx` save / new / delete | `rule.write` | §4 #8 |
| B1.2 | Event acknowledge / resolve | `PATCH /events/:publicId/status` | `EventDetail.tsx` | `event.write` | §4 unguarded |
| B1.3 | CMDB CI edit | `PATCH /cis/:publicId` | `CMDBDetail.tsx` | `cmdb.write` | §3 |
| B1.4 | Incident workflow batch | `POST /incidents/:publicId/promote-major`, `PATCH /incidents/:publicId/assign`, `PATCH /incidents/:publicId/links`, `POST /incidents/:incidentId/watchers`, `DELETE /incidents/:incidentId/watchers/:userId` | `IncidentDetail.tsx` | `incident.write` (+ `incident.major` for promote) | §1 |
| B1.5 | KB article create/edit/publish | `POST /kb/articles`, `PATCH /kb/articles/:publicId`, `PATCH /kb/articles/:publicId/status` | `KBEditor.tsx`, `ArticleView.tsx` | `kb.write` | §6 |

### Batch 2 (complete)

Audit §2 (Changes) and §5 (Requests) remainder. Status transitions (submit/schedule/implement) and PIR submission were dropped from B2 scope after re-reading the audit — those flows aren't surfaced in the current UI and aren't flagged in `mutation-audit.md`.

| # | Subject | Endpoints | UI | Permission key(s) |
|---|---------|-----------|----|-------------------|
| B2.1 | Change reschedule | `PATCH /changes/:publicId/reschedule` | `RescheduleModal` | `change.write` |
| B2.2 | Request lifecycle (cancel, reassign, watchers) | `PATCH /requests/:publicId/cancel`, `PATCH /requests/:publicId/steps/:stepId/reassign`, `POST /requests/:publicId/watchers`, `DELETE /requests/:publicId/watchers/:userId` | `RequestDetail.tsx` cancel/reassign/watcher modals | `request.write` |

### Batch 4 (complete)

Audit §1 IncidentQueue bulk ops. The status mutation endpoint already existed (B1.2 `/incidents/:publicId/status`); B4 added the missing piece (priority + tags edit) and rewired the queue UI.

| # | Subject | Endpoints | UI | Permission key(s) |
|---|---------|-----------|----|-------------------|
| B4.1 | Incident priority + tags edit | `PATCH /incidents/:publicId` | — | `incident.write` |
| B4.2 | IncidentQueue bulk + row UI | (reuses existing endpoints) | `IncidentQueue.tsx` bulk-close, bulk-assign, bulk-priority, bulk-tag, row assign-to-me — Promise.allSettled with banner | `incident.write` |

### Batch 5 (complete)

Audit §1 MajorIncidentWarRoom + Top-10 #3 (stand-down legally requires reason).

| # | Subject | Endpoints | UI | Permission key(s) |
|---|---------|-----------|----|-------------------|
| B5.1 | War-room endpoints | `POST /incidents/:publicId/stand-down`, `POST /incidents/:publicId/comms` | — | `incident.write` (`incident.major` key not yet in seedRbac.ts; documented inline) |
| B5.2 | War-room UI | (reuses B5.1 + B1.4 + resolve) | `MajorIncidentWarRoom.tsx` — handlePostComms, handleStandDown, handleResolve, LinkChange/LinkProblem modals | `incident.write` |

AddCommenter intentionally deferred — left as a TODO; commenters can be wired via the existing watcher endpoint with a role marker as a follow-up.

### Batch 7 (complete)

Audit §4 monitoring rule editor — mirrors B1.1 alert-routes for rules.

| # | Subject | Endpoints | UI | Permission key(s) |
|---|---------|-----------|----|-------------------|
| B7 | Monitoring rule CRUD | `POST/PATCH/DELETE /monitoring/rules` | `MonitoringRules.tsx` wizard + toggle + delete | `rule.write` |

### Future batches (deferred)

- **B6** Admin user provisioning (create/edit user) — deferred. Current client `useCurrentUser` context manages an in-memory RBAC user hierarchy (division/department/team/level/functionalRoles) that doesn't map cleanly to the server's Prisma `User` model. Wiring this requires a schema migration to surface those fields server-side; the audit flags this as low priority (read-only catalog is acceptable for current users).

## Execution mode

Using `superpowers:subagent-driven-development` from this controller session. Per task: fresh implementer subagent (full task text + scene context inlined) → spec reviewer subagent → code quality reviewer subagent → mark complete → next.

## Verification (per task)

- `npm run lint` clean (src + server tsconfig)
- `npm test` all tests pass; new domain test file present
- Audit-line status flips from 🔴 to ✅ in the closeout block at the bottom of M6.md

## Closeout

Each task appends a one-paragraph closeout (endpoints + repo functions + test count delta) to this file. Cumulative endpoint count and test count delta tracked in a table at the bottom.

## Progress

| Task | Endpoints | Tests added | Status |
|------|-----------|-------------|--------|
| B1.1 AlertRouting | 3 (POST/PATCH/DELETE `/monitoring/routes`) | 11 | ✅ |
| B1.2 Event status | 1 (PATCH `/events/:publicId/status`) | 6 | ✅ |
| B1.3 CMDB edit | 1 (PATCH `/cis/:publicId`) | 6 | ✅ |
| B1.4 Incident batch | 5 endpoints (`/promote-major`, `/assign`, `/links`, `/watchers` POST + DELETE) | 22 | ✅ |
| B1.5 KB writes | 3 (POST `/kb/articles`, PATCH `/kb/articles/:publicId`, PATCH `/kb/articles/:publicId/status`) | 12 | ✅ |
| B2.1 Change reschedule | 1 (PATCH `/changes/:publicId/reschedule`) | 8 | ✅ |
| B2.2 Request lifecycle | 4 (PATCH cancel + PATCH reassign + POST/DELETE watchers) | 25 | ✅ |
| B4.1 Incident edit | 1 (PATCH `/incidents/:publicId`) | 10 | ✅ |
| B4.2 IncidentQueue bulk UI | (reuses endpoints) | 0 | ✅ |
| B5.1 War-room endpoints | 2 (POST `/incidents/:publicId/stand-down`, POST `/incidents/:publicId/comms`) | 21 | ✅ |
| B5.2 War-room UI | (reuses endpoints) | 0 | ✅ |
| B7 Monitoring rule CRUD | 3 (POST/PATCH/DELETE `/monitoring/rules`) | 18 | ✅ |
| B6 Admin user provisioning | — | — | ⏭ deferred |

## Batch 1 closeout

All five Batch 1 tasks are ✅. The audit's "Top 5 critical" mutations are wired, every unguarded handler the audit flagged has a `requirePermission(...)` route behind it, and KB persistence — the last 0%-coverage domain after B1.3 closed CMDB — is wired through create / edit / publish.

| Task | Endpoints | Tests added |
|------|-----------|-------------|
| B1.1 AlertRouting writes        | 3 (POST/PATCH/DELETE `/monitoring/routes`)                                                             | 11 |
| B1.2 Event status               | 1 (PATCH `/events/:publicId/status`)                                                                   |  6 |
| B1.3 CMDB CI edit               | 1 (PATCH `/cis/:publicId`)                                                                             |  6 |
| B1.4 Incident workflow batch    | 5 (`/promote-major`, `/assign`, `/links`, watchers POST + DELETE)                                      | 22 |
| B1.5 KB writes                  | 3 (POST/PATCH/PATCH-status under `/kb/articles`)                                                       | 12 |
| **Cumulative**                  | **13 new endpoints**                                                                                   | **57** |

## Batch 2 + 4 + 5 + 7 closeout

Batches 2, 4, 5, and 7 all landed on `feat/m6.11-batch-2`. The mutation-audit's remaining critical surface — change reschedule, request cancel/reassign/watchers, IncidentQueue bulk operations, war-room stand-down/comms/links/resolve, and monitoring rule CRUD — is wired. The audit's "Top 10" list is fully closed (the last two items, #3 war-room stand-down with reason and #9 unguarded event endpoints, both land in this set).

| Task | Endpoints | Tests added |
|------|-----------|-------------|
| B2.1 Change reschedule           | 1 (`PATCH /changes/:publicId/reschedule`)                                                              |  8 |
| B2.2 Request lifecycle           | 4 (cancel + reassign + watchers POST/DELETE)                                                           | 25 |
| B4.1 Incident edit               | 1 (`PATCH /incidents/:publicId`)                                                                       | 10 |
| B4.2 IncidentQueue bulk UI       | UI-only (reuses status / assign / update endpoints)                                                    |  — |
| B5.1 War-room endpoints          | 2 (stand-down + comms)                                                                                 | 21 |
| B5.2 War-room UI                 | UI-only (reuses stand-down / comms / resolve / setLinks)                                               |  — |
| B7  Monitoring rule CRUD         | 3 (POST/PATCH/DELETE `/monitoring/rules`)                                                              | 18 |
| **Cumulative (batch 2/4/5/7)**   | **11 new endpoints**                                                                                   | **82** |
| **Total M6.11 (all batches)**    | **24 new endpoints**                                                                                   | **139** |

Test count: 147 → 285 (only the pre-existing `changes-writes.test.ts` P2002 flake against a populated dev DB; reseeding `npx tsx prisma/seed.ts` makes the full suite green).

**B6 deferred** — admin user provisioning needs a Prisma schema migration to surface the client's `division/department/team/level/functionalRoles` hierarchy server-side, plus the matching POST/PATCH/DELETE under `/admin/users`. Tracked separately; audit flags as low priority.

Endpoint count overall: 9 → 22. Test count: 147 → 204 (only the pre-existing `changes-writes.test.ts` P2002 flake fails against the populated dev DB; all 57 new B1 tests green).
